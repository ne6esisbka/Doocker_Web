# Doocker_Web
# Simple Python Web Stack (Nginx + Python)

Реализация отказоустойчивой связки из кастомного Python-сервера и Nginx в качестве Reverse-proxy. Проект полностью контейнеризирован и настроен для автоматического развертывания.

## 🛠 Технологический стек
*   **Reverse Proxy:** Nginx (чистая настройка конфига без сторонних решений).
*   **Backend:** Python 3.9 (встроенный `http.server`).
*   **Infrastructure:** Docker, Docker Compose.

## 🏗 Архитектура
*  **Nginx (Frontend):** Принимает входящий трафик на порту `80`. Проксирует запросы на бэкенд, обрабатывает ошибки (кастомная 404 страница) и управляет заголовками.
*  **Python Server (Backend):** Изолированный контейнер, отдающий статику на порту `8080`. Недоступен напрямую из внешней сети.

**Особенности:**
*   Реализованы **Healthchecks**: Nginx не запустится, пока Python-сервер не подтвердит готовность.
*   **Авторестарт**: Настроена политика `on-failure` для обеспечения высокой доступности.
*   **Лимиты ресурсов:** Ограничение памяти до 512MB для каждого сервиса.

## 🚀 Быстрый запуск

### Предварительные требования
*   Установленный **Docker** и **Docker Compose**.

### Развертывание
* **Клонируйте репозиторий:**
`
    $ git clone https://github.com/ne6esisbka/Doocker_Web.git
    $ cd Doocker_Web
`
* **Запустите проект в фоновом режиме:**

>   $ docker compose up -d --build

* **Проверьте статус контейнеров:**

 >   $ docker ps

* **Подключение к ресурсу**

  >  $ http://python_server:80/

Вы должны увидеть статус **(healthy)** у обоих контейнеров.
📂 Структура проекта
 *   /backend — Исходный код сервера и Dockerfile для сборки образа.
 *   /nginx — Конфигурационные файлы Nginx и шаблоны страниц ошибок.
 *   compose.yml — Описание оркестрации всей системы.
📝 Конфигурация Nginx
В проекте используется ручная настройка nginx.conf.
**Основные моменты:**
* Перенаправление запросов: proxy_pass http://python_server:8080;
* Проброс реальных IP-адресов клиентов через заголовки X-Real-IP.
* Обработка 404 ошибки через локальный файл.
Разработано в рамках демонстрации навыков администрирования Nginx и Docker.

---
